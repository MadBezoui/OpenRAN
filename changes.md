
Here is a summary of the major experiments, code changes, and the resulting files from our session:

### 1. Major Experiments & Code Changes

* **Dynamic Scale Experiment:** Modified `scripts/run_dynamic_scale.py` to use a `PersistentCPSAT` approach. We replaced deprecated callback mechanisms with modern OR-Tools `CP-SAT` methods (`ClearAssumptions()` and `AddAssumptions()`) to properly evaluate the baseline sequential repair performance.
* **Static Pipeline & Certificates:** Updated `src/experiments/pipeline.py` to implement the `forward_check` logic. This ensures that the system correctly generates and emits **audit certificates** for forwarding decisions and blocked actions.
* **Structural Sensitivity Experiment:** Created a completely new script (`scripts/run_structural.py`) to systematically evaluate how graph density and domain size impact the solver's latency and repair core sizes.

### 2. Major Manuscript Revisions (`main6.tex`)

* **Length & Tone Reduction:** Reduced the manuscript length by 15-20% by cutting repetitive limitations and tightening the prose.
* **Scientific Claims:** Refined the core claims. Instead of broadly claiming the solver is "faster," it now accurately states that it *outperformed the evaluated Python rebuild-and-resolve implementations*. We also clarified that the pipeline uses a single deterministic worker, and that the audit certificates provide *logical replayability without tamper-evidence*.
* **Syntax & Compilation Fixes:** Cleaned up problematic punctuation (rogue em-dashes `---` and textual semicolons `;`) that were breaking the LaTeX compiler, particularly within TikZ and math blocks.
* **References & Figure Paths:** Fixed corrupted figure paths caused by directory restructurings, and successfully ran the full `pdflatex` $\rightarrow$ `bibtex` $\rightarrow$ `pdflatex` pipeline to perfectly resolve all undefined citations and cross-references.

### 3. Key Resulting Files

* **`artifacts/raw/`**: Contains the raw data outputs (JSON/CSV) generated by the newly executed experiments.
* **`figures/` & `artifacts/figures/`**: Contains the generated and updated PDF plots visualizing the results (e.g., `fig1_reduction.pdf`, `fig_core_sizes.pdf`, `fig_coldstart.pdf`).
* **`main6.tex`**: The heavily revised, syntactically clean LaTeX source code.
* **`main6.pdf`**: The final, perfectly compiled manuscript containing all the new results, plots, and corrected bibliography.
